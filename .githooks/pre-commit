#!/usr/bin/env sh
set -eu

if command -v make >/dev/null 2>&1; then
  echo "Running lint..."
  make lint

  echo "Running test..."
  make test
else
  echo "make is required for pre-commit checks" >&2
  exit 1
fi
